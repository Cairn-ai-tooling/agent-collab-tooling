> Synced from `{artifact_path}` by `/github-backlog-sync`. Edit the **artifact**, not this issue
> body — it is regenerated on each sync. Discussion belongs in the comments below (never touched).

**Type:** {type} · **Status:** {status} · **Parent:** {parent} · **Artifact:** `{id}`

{body}
